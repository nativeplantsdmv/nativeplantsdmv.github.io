# Sanity API configuration — set during build (GitHub Actions, CF Pages, etc.)
# Create a project at https://www.sanity.io/manage
SANITY_PROJECT_ID=z9dgpdy9
SANITY_DATASET=production
# API token for authenticated Sanity queries (used by the agent)
# Create at https://www.sanity.io/manage/z9dgpdy9/api
SANITY_API_TOKEN=
